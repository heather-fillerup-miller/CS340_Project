<h2>{{title}} <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Add</button></h2>

<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Customer ID</th>
            <th scope="col">License Plate</th>
            <th scope="col">Make</th>
            <th scope="col">Model Name</th>
            <th scope="col">Model Year</th>
            <th scope="col" colspan= "2">
            <form class="form-inline">
                <input class="form-control mr-sm-2" type="search" aria-label="Search">
                <button class="btn btn-outline-info my-2 my-sm-0" type="submit">Search</button>
            </form>
            </th>
        </tr>
    </thead>

    {{#if dataRows}}
    <tbody>
        {{#each dataRows}}
        <tr id= "{{this.id}}">
            <th scope="row">{{this.id}}</td>
             <th scope="row">{{this.customer_id}}</td>
            <td>{{this.license_plate}}</td>
            <td>{{this.make}}</td>
            <td>{{this.model_name}}</td>
            <td>{{this.model_year}}</td>
            <td><button type="button" class="btn btn-outline-warning">Update</button>
            <button type="button" class="btn btn-outline-danger">Delete</button></td>
        </tr>
        {{/each}}
    </tbody>
    {{/if}}
</table>
<br>
